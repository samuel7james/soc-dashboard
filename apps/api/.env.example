NODE_ENV=development
PORT=4000
HOST=0.0.0.0
LOG_LEVEL=info
CORS_ORIGIN=http://localhost:3000

# Required — see docker-compose.yml for local Postgres/Redis
DATABASE_URL=postgresql://soc:soc_dev_password@localhost:5432/soc_platform?schema=public
REDIS_URL=redis://localhost:6379

# Required — generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_ACCESS_SECRET=
