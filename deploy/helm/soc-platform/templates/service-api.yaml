apiVersion: v1
kind: Service
metadata:
  name: {{ include "soc-platform.fullname" . }}-api
  labels:
    {{- include "soc-platform.componentLabels" (merge (dict "component" "api") .) | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "soc-platform.componentSelectorLabels" (merge (dict "component" "api") .) | nindent 4 }}
