apiVersion: 1

providers:
  - name: soc-platform
    orgId: 1
    folder: SOC Platform
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
