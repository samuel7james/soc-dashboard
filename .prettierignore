node_modules
.next
dist
.turbo
pnpm-lock.yaml
coverage
test-results
playwright-report

# Helm chart templates are YAML-with-Go-template-directives, not valid
# standalone YAML — Prettier's YAML parser chokes on the {{ }} syntax.
deploy/helm/**/templates/**

# Terraform has its own formatter (terraform fmt), already run/verified.
deploy/terraform/**/*.tf
