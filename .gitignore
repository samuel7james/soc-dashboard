# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Environment variables
.env
.env.local
.env.test
.env.production
.env*.local

# Build outputs
.next/
dist/
build/
out/
.turbo/
*.tsbuildinfo

# Test/coverage
coverage/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Logs
logs
*.log

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# Terraform (.terraform.lock.hcl is NOT ignored — like a lockfile, it should be committed)
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log 
# Prisma 7 generates the client into the package source tree (see
# packages/database/prisma/schema.prisma). It is produced by postinstall,
# so it is build output, not source.
packages/database/src/generated/
